# CS2 分析系統實作說明

本文件詳細說明了「CS2 賽事數據與飾品投資分析系統」的運作流程、技術實作細節以及未來的擴充方向。

## 1. 系統運作流程

系統採用 MVC 架構設計，將不同職責分離，確保程式碼的可維護性：

- **使用者發送請求**：使用者透過瀏覽器在前端介面（如：首頁、新增個人投資組合的表單）點擊或輸入資訊並送出。
- **Controller (Flask Route) 接收與驗證**：Flask 後端接收 HTTP 請求 (GET/POST)，驗證使用者的登入狀態 (Session) 以及表單欄位資料格式的正確性。
- **Model (資料邏輯) 處理**：Flask 路由調用對應的 Model 函式（例如：查詢最新飾品價格、執行 `add_portfolio_item` 等）。
- **資料庫 (SQLite) 操作**：Model 負責執行對應的 SQL 指令（CRUD 操作），從資料庫寫入或讀取所需數據，並將結果回傳給 Model，再轉為 Python 物件交還給 Controller。
- **View (Jinja2) 渲染與回應**：Controller 取得資料後，將變數注入 Jinja2 模板中生成動態 HTML，最後透過 HTTP Response 回傳給使用者的瀏覽器呈現。對於圖表類需求，則傳遞 JSON 數據至前端供 JS 繪圖。

## 2. 技術實作細節與資料流動

- **單體架構搭配 SSR (Server-Side Rendering)**：
  為加速開發並降低初期系統複雜度，專案採用 Flask 後端統一控制並在伺服器端渲染 HTML (Jinja2) 的方式。這不僅對 SEO 友善，也加快了首頁等靜態資訊載入速度。
- **資料庫設計 (SQLite)**：
  使用輕量級的 SQLite 作為資料儲存中心，檔案獨立便於管理。針對高頻查詢的資料表（如價格走勢與戰績）皆有考量建立索引。設計了包含 `User` (會員)、`Items` (飾品)、`Esports` (賽事) 與 `Portfolios` (投資組合) 等多個關聯資料表。
- **路由模組化 (Flask Blueprints)**：
  透過 `routes/` 目錄將不同業務邏輯拆分為獨立的 Blueprint（如 `auth_routes.py`, `esports_routes.py`, `items_routes.py`），避免單一檔案過於臃腫，大幅提升程式碼維護性。
- **動態圖表呈現**：
  飾品的歷史價格走勢圖表等複雜視覺需求，採用在 Jinja 模板中預留 API 接口的方式，後端提供 JSON 格式的歷史數據，再交由前端的輕量級 JS 圖表套件（如 Chart.js）進行動態渲染，確保高度互動性與系統效能。
- **安全性實作**：
  使用者密碼在寫入資料庫前必須經過 Hash 處理；透過 Flask 內建模板過濾機制與參數化查詢 (Parameterized Query)，有效防止 XSS 與 SQL Injection 攻擊。

## 3. 未來擴充方向

儘管目前系統已設計了核心功能的實作框架，但為了因應未來實際上線的流量與更進階的應用情境，規劃以下擴充方向：

1. **資料庫升級與快取機制**：
   隨著資料量與併發請求增加，未來可從 SQLite 平滑遷移至 PostgreSQL 或 MySQL。此外，可引入 Redis 作為快取層，將高頻存取的市場價格與排行榜快取在記憶體中，大幅降低關聯式資料庫負擔。
2. **非同步任務處理**：
   針對「到價提醒」功能的實作，未來計畫導入 Celery 與 RabbitMQ/Redis 進行背景非同步任務處理，實現系統自動定時輪詢最新價格並主動發送通知（Email 或推播），避免阻塞主執行緒。
3. **即時賽況與數據 API 串接**：
   目前資料規劃依賴系統內部手動/定時收集數據，未來將串接外部第三方 API（如 HLTV 賽事數據或 Steam Market API），實現自動化的即時賽況更新與飾品價格同步，提高資料精確度與即時性。
4. **前後端分離與微服務化**：
   若系統規模持續擴大且有開發手機版 App 的需求，未來將考慮全面重構為前後端分離架構。Flask 可轉型專注於提供 RESTful API，前端則交由 React 或 Vue 等現代化框架處理，甚至將賽事與市場拆分為獨立的微服務，提升狀態管理的彈性與系統可用性。
